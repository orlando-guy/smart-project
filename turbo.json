{
  "$schema": "https://turbo.build/schema.json",
  "globalPassThroughEnv": ["API_PORT"], 
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}